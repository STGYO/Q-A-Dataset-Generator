# Core
typer[all]>=0.12.0
rich>=13.0.0
pandas>=2.0.0

# Web UI
streamlit>=1.35.0

# LLM – Online
openai>=1.30.0

# LLM – Offline (Ollama uses OpenAI-compatible API, no extra dep)
# google-generativeai>=0.7.0  # Uncomment for Google Gemini support

# PDF extraction (install one or both)
pymupdf>=1.24.0          # PyMuPDF (fitz) – preferred
# pypdf>=4.0.0           # fallback

# Text splitting
langchain-text-splitters>=0.2.0

# Dev / test
pytest>=8.0.0
pytest-cov>=5.0.0
